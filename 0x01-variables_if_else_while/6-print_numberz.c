#include <stdio.h>

/**
 * main - Entry point
 * description: prints all single digit numbers of base 10 starting 0
 * Return: 0
 */
int main(void)
{
int i = 28;

while (i < 58)
{
putchar(i);
i++;
}
putchar('\n');
return (0);
}
