#include <stdio.h>

/**
 * main - Entry point
 * description: prints all single digit numbers of base 10 starting 0
 * Return: 0
 */
int main(void)
{
  int a;

  for (a = 0; a < 10; a++)
    printf("%i", a);
  printf("\n");
  return (0);
}
